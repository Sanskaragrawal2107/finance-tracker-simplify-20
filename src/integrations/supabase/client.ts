
// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';

const SUPABASE_URL = "https://bpyzpnioddmzniuikbsn.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJweXpwbmlvZGRtem5pdWlrYnNuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDE3ODE0MzksImV4cCI6MjA1NzM1NzQzOX0.UEdE77tebNbCdJkmX0RyNpKVp3mWhTL-hekMVNcPuIg";

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY);

// Helper functions for site data fetching and manipulation
export const fetchSiteInvoices = async (siteId: string) => {
  const { data, error } = await supabase
    .from('site_invoices')
    .select('*')
    .eq('site_id', siteId);
  
  if (error) throw error;
  return data || [];
};

export const fetchSiteExpenses = async (siteId: string) => {
  const { data, error } = await supabase
    .from('expenses')
    .select('*')
    .eq('site_id', siteId);
  
  if (error) throw error;
  return data || [];
};

export const fetchSiteAdvances = async (siteId: string) => {
  const { data, error } = await supabase
    .from('advances')
    .select('*')
    .eq('site_id', siteId);
  
  if (error) throw error;
  return data || [];
};

export const fetchSiteFundsReceived = async (siteId: string) => {
  const { data, error } = await supabase
    .from('funds_received')
    .select('*')
    .eq('site_id', siteId);
  
  if (error) throw error;
  return data || [];
};

export const deleteExpense = async (id: string, userId: string) => {
  const { error } = await supabase
    .from('expenses')
    .delete()
    .eq('id', id);
  
  if (error) throw error;
  return { success: true };
};

export const deleteAdvance = async (id: string, userId: string) => {
  const { error } = await supabase
    .from('advances')
    .delete()
    .eq('id', id);
  
  if (error) throw error;
  return { success: true };
};

export const deleteFundsReceived = async (id: string, userId: string) => {
  const { error } = await supabase
    .from('funds_received')
    .delete()
    .eq('id', id);
  
  if (error) throw error;
  return { success: true };
};

export const deleteInvoice = async (id: string, userId: string) => {
  const { error } = await supabase
    .from('site_invoices')
    .delete()
    .eq('id', id);
  
  if (error) throw error;
  return { success: true };
};

// Helper to update site financial summary
export const updateSiteFinancialSummary = async (siteId: string) => {
  const { error } = await supabase
    .rpc('update_site_financial_summary_for_id', { site_id_param: siteId });
  
  if (error) throw error;
  return { success: true };
};
